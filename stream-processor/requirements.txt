# Host-side tooling for the stream processor, pinned exactly.
#
# fraud_job.py runs INSIDE the cluster: its PyFlink, ONNX Runtime and redis
# versions come from infra/flink/Dockerfile, not from this file. On the host,
# experiments/ and the tests need pandas, redis and pytest.
#
# apache-flink is listed only so fraud_job.py can be imported locally, and it
# MUST match the cluster's 1.19.1: a host copy of another major version would
# validate the job against an API the cluster does not have.
#
# Install with:  pip install -r requirements.txt
apache-flink==1.19.1
redis==5.3.1
pandas==2.2.3
pytest==9.0.2
